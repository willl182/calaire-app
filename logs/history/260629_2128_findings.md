# Technical Finding / Milestone: Estado de Fase 2 de migración T3

**Context**: Migración estructural del repo `calaire-app` al layout T3 (`src/app/`, `src/server/`, etc.).

## The Discovery

Al consultar el estado de la Fase 2 se encontró que:

1. **Fase 0 y Fase 1 ya están commiteadas** en la rama `feature/t3-estructura-segura`:
   - `996fc97 t3(fase1): src/env.js, src/lib/utils.ts y scripts vitest`
   - `33fc424 t3(fase0): andamiaje, deps e inventarios de seguridad`

2. **Fase 2 está parcialmente ejecutada**:
   - `app/components/` ya se movió a `src/components/`.
   - `app/` ya se copió a `src/app/`.
   - Se crearon puentes temporales en `src/lib/*.ts`.
   - **No se completó la corrección de imports internos**: aún quedan 17 referencias a `@/app/...` dentro de `src/app/`.
   - **No se activó el nuevo alias**: `tsconfig.json` sigue con `"@/*": ["./*"]`.
   - **No se eliminó `app/` de raíz**.

3. **Build rojo en el estado actual**:
   ```
   ./src/app/(protected)/ParticipantTopNav.tsx:3:26
   Type error: Cannot find module '@/components/LogoUnal'
   ```
   Causa: `src/app/` ya usa el alias nuevo (`@/components/LogoUnal`), pero `tsconfig.json` todavía apunta `@/*` a raíz, donde no existe `components/LogoUnal`.

## Key Files Affected

- `tsconfig.json` — debe cambiar el alias a `"@/*": ["./src/*"]`.
- `src/app/` — debe corregir 17 imports `@/app/...` a `@/...`.
- `app/` — debe eliminarse una vez `src/app/` esté activo.
- `src/lib/*.ts` — puentes temporales; se eliminarán en Fase 3.

## Next Steps

1. Corregir imports `@/app/` → `@/` en `src/app/`.
2. Actualizar `tsconfig.json`.
3. Eliminar `app/`.
4. Verificar `pnpm build && pnpm lint && pnpm test:e2e`.
5. Commitear Fase 2.
