# Technical Finding / Milestone

**Context**: Codigo aleatorio automatico por participante, Fase 4.

## Implementation

La visibilidad del codigo automatico quedo ajustada en UI:

- `app/(protected)/dashboard/rondas/[id]/participantes/page.tsx` muestra el codigo con etiqueta explicita `Codigo` en cada cupo.
- La misma fila usa el badge de perfil, por lo que la referencia (`member_special`) tambien muestra su codigo automatico.
- `app/(protected)/dashboard/rondas/[id]/configuracion-pt/page.tsx` aclara que el codigo analitico (`participant_id`) se genera automaticamente.
- La edicion manual del codigo queda visible solo como respaldo administrativo, junto al campo de replica para completar el CSV.

## Verification

`pnpm lint` paso sin errores. Persisten 7 warnings no relacionados en dashboard/resultados y `convex/_generated/*`.

## Next Implementation Note

Continuar con Fase 6 (validacion/exportacion PT) y luego Fase 7 (flujo integrado).
