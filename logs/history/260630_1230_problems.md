# Problem Record (Troubleshooting)

**Severity**: Medium

## The Problem

Dos verificaciones de Fase 5 no pudieron completarse por condiciones externas al cambio de codigo:

- `timeout 30s pnpm exec convex dev` fallo con `Unexpected error when authorizing - are you connected to the internet?` y `TypeError: fetch failed`.
- `PLAYWRIGHT_START_SERVER=1 pnpm exec playwright test tests/e2e/app.spec.ts --project=chromium` fallo por timeout del `config.webServer`; el web server repite `Error: Can't resolve 'tailwindcss' in '/home/w182/w421'`.

## The Solution / Workaround

No se aplico workaround durante Fase 5 porque:

- `pnpm exec convex codegen` ya habia validado y subido funciones correctamente.
- El problema de Playwright/Tailwind ya estaba documentado antes de esta fase y no fue introducido por los movimientos Convex.

## Learnings to Preserve

- No interpretar el fallo de e2e como regresion de Fase 5 sin aislar primero la resolucion de Tailwind desde `/home/w182/w421`.
- No interpretar el fallo de `convex dev` como error de funciones mientras sea un fallo de autorizacion/red previo al arranque.
