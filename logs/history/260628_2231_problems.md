# Problem Record: Review pendiente PR SGC Maestro protv2

**Severity**: Medium/High

## The Problem

El PR `https://github.com/willl182/calaire-app/pull/3` quedo abierto para code review, pero CodeRabbit/terminal reporto varios hallazgos que conviene resolver antes de mergear. El usuario pidio explicitamente no hacer commit ni push adicional en este momento.

## User-Requested Correction

- Usar `saver`.
- No hacer commit.
- No hacer push.
- Dejar memoria para continuar desde el estado de review.

## Current Git State

- Rama: `feature/sgc-maestro-protv2`.
- PR: `#3`.
- Commit actual del PR: `7b0d1be feat: implementa SGC maestro protv2`.
- Estado observado: rama sincronizada con `origin/feature/sgc-maestro-protv2`.
- Archivo no trackeado observado: `cr-rev.md`.

## Review Findings To Address

Prioridad alta:

- `app/(protected)/dashboard/sgc/documentos/actions.ts`: mover redirects de exito fuera de `try/catch`; validar `documento_id` antes de subir archivo.
- `app/(protected)/dashboard/sgc/normativa/actions.ts`: mover redirect de exito fuera de `try/catch`.
- `app/(protected)/dashboard/sgc/documentos/[id]/page.tsx`: validar URLs editables/externas antes de persistir o renderizar; agregar labels programaticos a formularios.
- `app/(protected)/dashboard/sgc/documentos/[id]/versiones/[versionId]/download/route.ts`: verificar que `versionId` pertenece al documento `id`.
- `app/(protected)/dashboard/sgc/mapa/page.tsx`: tratar `externalUrl` como dato no confiable y renderizar link solo para `http(s)`.
- `convex/schema.ts` y `convex/sgc/maestro.ts`: hacer lookups de requisitos version-aware con indice por `norma`, `versionNorma`, `clausula`.
- `convex/sgc/maestro.ts`: validar existencia de documento/requisito antes de `documentoRequisitos`; validar `versionBaseId` pertenece al documento; rechazar codigo/nombre vacios en registros derivados; de-duplicar versiones oficiales; corregir filtro de requisitos pendientes sin relacion.
- `convex/sgc/maestro.ts`: revisar importacion de mapa para no borrar relaciones curadas en import generico y para que imports no-replacement sean idempotentes.

Prioridad menor:

- `scripts/import-sgc-seeds.mjs`: loggear `result.error` cuando `spawnSync` no lanza.
- `mapa_navegacion_sgc_pea.html`: activar nodos con Space ademas de Enter.
- `app/(protected)/dashboard/sgc/prototype/page.tsx`: mapear estados `activa`/`cerrada` y reservar padding inferior para switcher fijo.
- `app/(protected)/dashboard/SidebarNav.tsx`: fallback activo para subrutas SGC no listadas.
- Tests screenshot SGC: esperar tabla antes de capturar.
- Docs: normalizar rutas/provenance y mover expedientes fuera del route list en `logs/plans/260628_1856_plan_sgc-maestro-protv2.md`.

## Learnings to Preserve

- No mezclar la preparacion del PR con merge directo.
- No hacer commits adicionales sin confirmacion del usuario.
- Tratar los hallazgos de seguridad/consistencia de CodeRabbit como bloqueadores de merge, aunque el MVP compile y el smoke pase.
